services:
  db:
    image: postgres:16-alpine
    ports:
      - "5434:5432"
    environment:
      POSTGRES_USER: stammbaum
      POSTGRES_PASSWORD: stammbaum
      POSTGRES_DB: stammbaum
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
